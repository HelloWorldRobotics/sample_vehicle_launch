/**:
  # 14 seater buggy
  # ros__parameters:
  #   wheel_radius: 0.325 # The radius of the wheel, primarily used for dead reckoning.
  #   wheel_width: 0.176 # The lateral width of a wheel tire, primarily used for dead reckoning.
  #   wheel_base: 2.830 # between front wheel center and rear wheel center
  #   wheel_tread: 1.880 # between left wheel center and right wheel center
  #   front_overhang: 1.0 # between front wheel center and vehicle front
  #   rear_overhang: 1.1 # between rear wheel center and vehicle rear
  #   left_overhang: 0.128 # between left wheel center and vehicle left
  #   right_overhang: 0.128 # between right wheel center and vehicle right
  #   vehicle_height: 1.710
  #   max_steer_angle: 0.70 # [rad]

  #TARS6 Ackermann Base
  ros__parameters:
    wheel_radius: 0.125 # The radius of the wheel, primarily used for dead reckoning.
    wheel_width: 0.075 # The lateral width of a wheel tire, primarily used for dead reckoning.
    wheel_base: 0.520 # between front wheel center and rear wheel center
    wheel_tread: 0.535 # between left wheel center and right wheel center
    front_overhang: 0.1 # between front wheel center and vehicle front
    rear_overhang: 0.1 # between rear wheel center and vehicle rear
    left_overhang: 0.01 # between left wheel center and vehicle left
    right_overhang: 0.01 # between right wheel center and vehicle right
    vehicle_height: 0.95
    max_steer_angle: 0.349 # [rad] = 20 degrees
